#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "PASS.h"

int main(int narg, char* argv[]) {

    FILE *file = fopen(argv[1], "r");

    if (argv < 2) {
        printf("Error opening file");
    }

}